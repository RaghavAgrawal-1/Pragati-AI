/**
 * DEMO DATA — not real, and deliberately thin. Model outputs, accuracy and
 * confidence are never invented; those fields stay empty until the ML service
 * returns them.
 */
export const MOCK_PREDICTIONS = { items: [], summary: null, model_version: null };
