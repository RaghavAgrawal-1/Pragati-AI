services:
  db:
    image: postgres:16-alpine
    container_name: pragati-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-pragati}
      POSTGRES_USER: ${POSTGRES_USER:-pragati}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change_me}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-pragati} -d ${POSTGRES_DB:-pragati}"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    container_name: pragati-backend
    environment:
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-pragati}:${POSTGRES_PASSWORD:-change_me}@db:5432/${POSTGRES_DB:-pragati}
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data: